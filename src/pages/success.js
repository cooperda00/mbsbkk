//Modules
import React from "react"
//Components
import Layout from "../components/Layout/Layout"

const GetInvolvedPage = () => (
  <Layout>
    <h1>Thanks for submitting, we will be in touch shortly</h1>
  </Layout>
)

export default GetInvolvedPage
